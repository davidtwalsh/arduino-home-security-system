# arduino-home-security-system

This repository contains an Arduino-based home security system with a backend service and React frontend.

## Arduino WiFi credentials

Arduino sketches require `Arduino/credentials.h` to connect to WiFi.

Rename Arduino\credentials.example.h to credentials.h and plug in your WIFI name and password.
